/**
 * `@freellm/ui` — design tokens (locked at Tick 7) + reusable component
 * primitives populated across Group 2 alongside the React Bits / shadcn /
 * Framer Motion / recharts mash-up.
 *
 * Kept as a workspace package so the admin app and any future SDK
 * playground can import tokens without vendor-locking everything into
 * `apps/web/src`.
 */
export * from './design-tokens.js';
